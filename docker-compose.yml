version: '3'
services:
  # vue-client:
  #   image: ./client
  #   command: npm run serve
  #   ports:
  #     - "8080:8080"

  node-server:
    image: api
    restart: always
    ports:
      - "9000:9000"
    # links:
    #   - mongo
  socket-io-server:
    image: socket
    restart: always
    ports:
      - "3000:3000"